version: 1
disable_existing_loggers: false

handlers:
  console:
    class: logging.StreamHandler
    stream: ext://sys.stdout

loggers:
  prod:
    level: DEBUG
    handlers: [console]
    propagate: no

root:
  level: INFO
  handlers: [console]
